name: CI/CD Pipeline

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  # 后端测试任务
  backend-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend
    strategy:
      matrix:
        python-version: [3.11]
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: Cache pip dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Install backend dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-cov ruff mypy

    - name: Run backend tests with coverage
      run: |
        pytest tests/ --cov=app --cov-report=xml --cov-report=html

    - name: Generate coverage report
      run: |
        pytest tests/ --cov=app --cov-report=term-missing

    - name: Run code quality checks
      run: |
        # Ruff检查
        ruff check .
        
        # Mypy类型检查
        mypy --package app || true

  backend-mysql-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend
    services:
      mysql:
        image: mysql:9
        ports:
          - 3306:3306
        env:
          MYSQL_ROOT_PASSWORD: root_password
          MYSQL_DATABASE: ai_teaching_assistant
          MYSQL_USER: ai_teaching
          MYSQL_PASSWORD: ai_teaching_dev
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -uroot -proot_password"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=12
    env:
      DATABASE_URL: mysql+aiomysql://ai_teaching:ai_teaching_dev@127.0.0.1:3306/ai_teaching_assistant
      TEST_DATABASE_URL: mysql+aiomysql://ai_teaching:ai_teaching_dev@127.0.0.1:3306/ai_teaching_assistant
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"

    - name: Cache pip dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-mysql-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-mysql-pip-

    - name: Install backend dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-cov ruff mypy

    - name: Wait for MySQL
      run: |
        python - <<'PY'
        import time
        import pymysql

        for attempt in range(30):
            try:
                conn = pymysql.connect(
                    host="127.0.0.1",
                    user="ai_teaching",
                    password="ai_teaching_dev",
                    database="ai_teaching_assistant",
                    port=3306,
                )
                conn.close()
                print("MySQL is ready")
                break
            except Exception as exc:
                if attempt == 29:
                    raise
                print(f"MySQL not ready yet: {exc}")
                time.sleep(2)
        PY

    - name: Run MySQL migrations
      run: |
        python -m alembic upgrade head

    - name: Run backend MySQL regression tests
      run: |
        pytest tests/test_students.py tests/test_submissions.py tests/test_crud.py tests/test_qa.py tests/test_file_upload.py -q

  # 前端测试任务
  frontend-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend
    strategy:
      matrix:
        node-version: [24.x]
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        cache-dependency-path: './frontend/package-lock.json'

    - name: Clean install frontend dependencies
      run: |
        if [ -d "node_modules" ]; then
          rm -rf node_modules
        fi
        if [ -f "package-lock.json" ]; then
          rm package-lock.json
        fi
        npm install --legacy-peer-deps
      shell: bash

    - name: Install additional dependencies
      run: |
        npm install ajv@^8.17.1 ajv-keywords@^5.1.0 --save-dev --legacy-peer-deps
        npm install i18next@^23.15.0 react-i18next@^14.1.3 --legacy-peer-deps
      working-directory: ./frontend

    - name: Run frontend tests
      run: npm test -- --passWithNoTests --coverage --watchAll=false

    - name: Run ESLint
      run: npx eslint src/ --ext .ts,.tsx --fix

    - name: Build frontend
      run: |
        echo "Building frontend application..."
        npm run build
      env:
        SKIP_PREFLIGHT_CHECK: true
        GENERATE_SOURCEMAP: false
        CI: false

  # Docker构建任务
  docker-build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [backend, frontend]
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-qemu-action@v3
      if: matrix.service == 'backend' || matrix.service == 'frontend'
      
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      if: matrix.service == 'backend' || matrix.service == 'frontend'

    - name: Login to DockerHub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
      if: github.event_name != 'pull_request' && (matrix.service == 'backend' || matrix.service == 'frontend')

    - name: Extract Docker metadata for backend
      id: meta_backend
      uses: docker/metadata-action@v5
      with:
        images: ${{ secrets.DOCKERHUB_USERNAME }}/ai-teaching-assistant-backend
      if: matrix.service == 'backend'

    - name: Extract Docker metadata for frontend
      id: meta_frontend
      uses: docker/metadata-action@v5
      with:
        images: ${{ secrets.DOCKERHUB_USERNAME }}/ai-teaching-assistant-frontend
      if: matrix.service == 'frontend'

    - name: Build and push backend Docker image
      uses: docker/build-push-action@v5
      with:
        context: ./backend
        file: ./backend/Dockerfile
        push: ${{ github.event_name != 'pull_request' }}
        tags: ${{ steps.meta_backend.outputs.tags }}
        labels: ${{ steps.meta_backend.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
      if: matrix.service == 'backend' && github.event_name != 'pull_request'

    - name: Build and push backend Docker image (no push for PR)
      uses: docker/build-push-action@v5
      with:
        context: ./backend
        file: ./backend/Dockerfile
        push: false
        tags: ai-teaching-assistant-backend:local
        cache-from: type=gha
        cache-to: type=gha,mode=max
      if: matrix.service == 'backend' && github.event_name == 'pull_request'

    - name: Build and push frontend Docker image
      uses: docker/build-push-action@v5
      with:
        context: ./frontend
        file: ./frontend/Dockerfile
        push: ${{ github.event_name != 'pull_request' }}
        tags: ${{ steps.meta_frontend.outputs.tags }}
        labels: ${{ steps.meta_frontend.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
      if: matrix.service == 'frontend' && github.event_name != 'pull_request'

    - name: Build and push frontend Docker image (no push for PR)
      uses: docker/build-push-action@v5
      with:
        context: ./frontend
        file: ./frontend/Dockerfile
        push: false
        tags: ai-teaching-assistant-frontend:local
        cache-from: type=gha
        cache-to: type=gha,mode=max
      if: matrix.service == 'frontend' && github.event_name == 'pull_request'

  # 部署任务 (仅在推送到main分支时运行)
  deploy:
    needs: [backend-test, backend-mysql-test, frontend-test, docker-build]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    steps:
    - name: Deploy to production
      run: echo "Deploying to production environment..."
      # 这里可以添加实际的部署步骤，例如：
      # - 部署到云平台
      # - 更新服务器上的容器
      # - 运行部署脚本等
